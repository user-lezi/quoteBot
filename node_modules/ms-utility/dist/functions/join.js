"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function default_1(arr, limit = arr.length, separator = ', ', and = false) {
    let str = '';
    for (let i = 0, len = limit > arr.length ? arr.length : limit; i < len; i++) {
        const elem = arr[i];
        if (i === 0) {
            str += elem;
        }
        else if (i + 1 === len) {
            str += (and ? ` and ${elem}` : `${separator}${elem}`);
        }
        else {
            str += separator + elem;
        }
    }
    return str;
}
exports.default = default_1;
//# sourceMappingURL=join.js.map