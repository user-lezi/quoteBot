import { Options } from "../typings/interfaces/Options";
import { TimeUnit } from "../typings/interfaces/TimeUnit";
export declare class Parser {
    #private;
    constructor(entries?: Array<[string, Omit<TimeUnit, "letter">]>);
    get uregex(): RegExp;
    advancedParseToMS(str: string): number;
    unsafeParseToMS(str: string): number;
    addTimeUnit(...entries: TimeUnit[]): this;
    addTimeUnit(entries: TimeUnit[]): this;
    addTimeUnit(entry: TimeUnit): this;
    isNumber(char: string): boolean;
    parseToString(ms: number, options?: Options): string;
    parseToMS(str: string): number;
    get container(): Map<string, TimeUnit>;
}
//# sourceMappingURL=Parser.d.ts.map